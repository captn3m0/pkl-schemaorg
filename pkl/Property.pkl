module org.schema.experimental
import "package://pkg.pkl-lang.org/pkl-pantry/pkl.experimental.uri@1.0.3#/URI.pkl" as URL
import "Intangible.pkl"
/// A property, used to indicate attributes and relationships of some Thing; equivalent
/// to rdf:Property.
///
/// See: <https://schema.org/Property>
/// Model depth: 3
open class Property extends Intangible.Intangible {
        
    /// Relates a property to a class that is (one of) the type(s) the property is expected to be
    /// used on.
    domainIncludes: (Listing<String>|String)?
        
    /// Relates a property to a property that is its inverse. Inverse properties relate the same
    /// pairs of items to each other, but in reversed direction. For example, the 'alumni' and
    /// 'alumniOf' properties are inverseOf each other. Some properties don't have explicit
    /// inverses; in these situations RDFa and JSON-LD syntax for reverse properties can be
    /// used.
    inverseOf: (Listing<Property|String>|Property|String)?
        
    /// Relates a property to a class that constitutes (one of) the expected type(s) for values
    /// of the property.
    rangeIncludes: (Listing<String>|String)?
        
    /// Relates a term (i.e. a property, class or enumeration) to one that supersedes it.
    supersededBy: (Listing<Property|String>|Property|String)?
    
}